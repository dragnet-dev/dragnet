title: 'Dragnet [Hunt]: {{.DragnetID}} — {{.Behaviour.ID}} Node.js reads /proc/*/mem for credential extraction'
id: {{.ID}}
status: {{.Status}}
description: |
  Detects Node.js or npm processes reading /proc/*/mem to extract in-memory OIDC tokens
  and other credentials. Behaviour: {{.Behaviour.ID}} — {{.Behaviour.Description}}
  Incident: {{.DragnetID}}
  {{.Description}}
references:
{{- range .References}}
  - {{.}}
{{- end}}
author: dragnet-bot
date: {{.Date}}
tags:
  - attack.t1003
  - attack.t1552.001
{{- range .Tags}}
  - {{.}}
{{- end}}
logsource:
  category: process_access
  product: linux
detection:
  selection:
    TargetImage|contains: '/proc/'
    TargetImage|endswith: '/mem'
    SourceImage|contains:
      - 'node'
      - 'npm'
  condition: selection
falsepositives:
  - Debugging tools reading process memory for legitimate diagnostics
level: {{.Level}}
